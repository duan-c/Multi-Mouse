# Normalize line endings for all text files.
* text=auto eol=lf

# Only include the addons folder when exporting to asset packages.
/**        export-ignore
/addons    !export-ignore
/addons/** !export-ignore
